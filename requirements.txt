# ============================================================
# Raspberry Pi 5 Rover Dependencies
# ============================================================

# Use official PyTorch CPU wheel repository (CRITICAL for Raspberry Pi):
# Prevents pip from downloading 3GB+ of useless NVIDIA CUDA packages (cudnn, cublas, etc.)
--extra-index-url https://download.pytorch.org/whl/cpu

# Web framework for the rover control interface and API
Flask>=3.0.0

# GPIO interface for dual IBT-2 motor driver control
gpiozero>=2.0

# Pin factory backend for gpiozero on Raspberry Pi OS (Bookworm / Trixie)
lgpio>=0.2.2.0

# Core numeric computation
numpy>=1.24.0

# Lightweight PyTorch CPU-only (ARM64)
torch>=2.2.0
torchvision>=0.17.0

# Computer Vision & Real-time YOLO Object Detection
ultralytics>=8.1.0

# Note on OpenCV & Picamera2:
# On Raspberry Pi OS, use system packages with --system-site-packages venv:
#   sudo apt install -y python3-picamera2 python3-opencv python3-gpiozero python3-lgpio
# If running outside system packages, uncomment below:
# opencv-python-headless>=4.8.0
# picamera2
